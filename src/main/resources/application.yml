server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/music?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost         # Redis ?????
    port: 6379              # Redis ????
    password:               # ?? Redis ????????????
    database: 0             # Redis ????????????? 0?
    timeout: 3000           # ???????ms?
    lettuce:
      pool:
        max-active: 8       # ????????
        max-idle: 8         # ???????
        min-idle: 0         # ???????
        max-wait: 1000      # ????????????ms?

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.musicapp.entity
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: true

